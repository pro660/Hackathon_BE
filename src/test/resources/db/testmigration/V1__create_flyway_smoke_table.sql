CREATE TABLE flyway_smoke_test (
                                   id BIGINT NOT NULL AUTO_INCREMENT,
                                   name VARCHAR(100) NOT NULL,
                                   PRIMARY KEY (id)
);
